volumes:
  - name: db-creds                
    secret:
      secretName: db-creds
      items:
        - key: keys.env
          path: .env               
volumeMounts:
  - name: db-creds                
    mountPath: /app/keys
    readOnly: true